<Project Sdk="Microsoft.NET.Sdk.Razor">

    <PropertyGroup>
        <TargetFramework>net5.0</TargetFramework>
        <Nullable>enable</Nullable>
        <RootNamespace>Integrant.Fundament</RootNamespace>
        <AssemblyName>Integrant.Fundament</AssemblyName>
    </PropertyGroup>
    
    <PropertyGroup>
        <PackageId>Integrant.Fundament</PackageId>
        <VersionPrefix>3.0.0</VersionPrefix>
        <VersionSuffix>alpha</VersionSuffix>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
        <DebugType>full</DebugType>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
        <DebugSymbols>true</DebugSymbols>
        <DebugType>full</DebugType>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="FlareSelect" Version="3.7.2-alpha" />
      <PackageReference Include="FlareTables" Version="3.8.1-alpha" />
      <PackageReference Include="Superset" Version="1.16.0-alpha" />
    </ItemGroup>

</Project>
